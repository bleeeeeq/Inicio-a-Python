# Perguntas sobre os números

num1 = float(input("Primeiro número: "))
num2 = float(input("Segundo número: "))

# Diferença e quadrado

dif = num1 - num2
result = (pow(dif,2))
print ("O resultado do quadrado da diferença do número 1 com o número 2 é: {}" .format (result))